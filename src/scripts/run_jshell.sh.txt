#!/bin/bash
# Move para o diretório pai (raiz)
cd "$(dirname "$0")/.."

echo "Compilando..."
javac lib/*.java QueryShell.java

if [ $? -eq 0 ]; then
    jshell --class-path .
else
    echo "Erro na compilação."
fi